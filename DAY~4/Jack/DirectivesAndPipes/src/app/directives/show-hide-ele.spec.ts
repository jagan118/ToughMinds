import { ShowHideEle } from './show-hide-ele';

describe('ShowHideEle', () => {
  it('should create an instance', () => {
    const directive = new ShowHideEle();
    expect(directive).toBeTruthy();
  });
});
