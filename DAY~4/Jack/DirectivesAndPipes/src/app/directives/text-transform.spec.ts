import { TextTransform } from './text-transform';

describe('TextTransform', () => {
  it('should create an instance', () => {
    const directive = new TextTransform();
    expect(directive).toBeTruthy();
  });
});
